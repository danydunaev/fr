# Проект: Карточка товара + REST API

Проект содержит две части:
1. Визуальная карточка товара с использованием SASS
2. REST API для управления товарами (CRUD операции)

## 📁 Структура проекта

```
fr/
├── app.js              # REST API сервер (Node.js + Express)
├── project.html        # HTML страница с карточкой товара
├── style.scss          # SASS стили (исходник)
├── style.css           # Скомпилированные CSS стили
├── package.json        # Зависимости Node.js
└── src/
    └── MirgantID.png   # Изображение товара
```

---

## 🎨 Часть 1: Карточка товара (SASS)

### Описание
Реализована карточка товара с применением:
- **Переменных SASS** (5 штук): цвета, отступы, радиус границ
- **Миксинов** (2 штуки): стилизация кнопок и теней
- **Вложенной структуры селекторов** (BEM методология)

### Структура карточки
Карточка содержит:
- **Фотографию** товара
- **Название** товара
- **Описание** товара
- **Цену**
- **Кнопку** "Купить"

### Используемые переменные
```scss
$primary-color: #3498db;      // Основной цвет (кнопка)
$secondary-color: #2c3e50;    // Цвет заголовка
$accent-color: #e74c3c;       // Цвет акцента (цена)
$border-radius: 12px;         // Радиус скругления
$spacing: 16px;               // Отступы
```

### Используемые миксины
```scss
@mixin button-style($bg-color, $hover-color)  // Стилизация кнопок
@mixin card-shadow                             // Тени карточки
```

### Компиляция SASS
```bash
sass style.scss style.css
```

### Просмотр
Откройте файл `project.html` в браузере.

---

## 🚀 Часть 2: REST API

### Описание
REST API для управления товарами с полным набором CRUD операций.

### Технологии
- **Node.js**
- **Express.js**
- **JSON** для обмена данными

### Структура объекта товара
```json
{
  "id": 1,
  "название": "Название товара",
  "стоимость": 1000
}
```

---

## 📡 API Endpoints

### 1. Получить все товары
```bash
GET /api/products
```

**Пример запроса:**
```bash
curl http://localhost:3000/api/products
```

**Ответ:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "название": "Разнорабочий",
      "стоимость": 0
    }
  ],
  "count": 1
}
```

---

### 2. Получить товар по ID
```bash
GET /api/products/:id
```

**Пример запроса:**
```bash
curl http://localhost:3000/api/products/1
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "название": "Разнорабочий",
    "стоимость": 0
  }
}
```

---

### 3. Добавить новый товар
```bash
POST /api/products
```

**Пример запроса:**
```bash
curl -X POST http://localhost:3000/api/products \
  -H "Content-Type: application/json" \
  -d '{"название":"Наушники AirPods Pro","стоимость":24990}'
```

**Тело запроса:**
```json
{
  "название": "Наушники AirPods Pro",
  "стоимость": 24990
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Товар успешно добавлен",
  "data": {
    "id": 4,
    "название": "Наушники AirPods Pro",
    "стоимость": 24990
  }
}
```

---

### 4. Обновить товар
```bash
PUT /api/products/:id
```

**Пример запроса:**
```bash
curl -X PUT http://localhost:3000/api/products/1 \
  -H "Content-Type: application/json" \
  -d '{"название":"Обновленное название","стоимость":5000}'
```

**Тело запроса (можно обновить только название или только стоимость):**
```json
{
  "название": "Обновленное название",
  "стоимость": 5000
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Товар успешно обновлён",
  "data": {
    "id": 1,
    "название": "Обновленное название",
    "стоимость": 5000
  }
}
```

---

### 5. Удалить товар
```bash
DELETE /api/products/:id
```

**Пример запроса:**
```bash
curl -X DELETE http://localhost:3000/api/products/1
```

**Ответ:**
```json
{
  "success": true,
  "message": "Товар успешно удалён",
  "data": {
    "id": 1,
    "название": "Разнорабочий",
    "стоимость": 0
  }
}
```

---

## 🛠️ Установка и запуск

### Установка зависимостей
```bash
npm install
```

### Запуск API сервера
```bash
node app.js
```

Сервер запустится на `http://localhost:3000`

### Компиляция SASS (если изменили стили)
```bash
sass style.scss style.css
```

---

## 📝 Тестирование API

### Вариант 1: curl (терминал)
Примеры команд смотрите в разделе "API Endpoints" выше.

### Вариант 2: Postman
1. Скачайте и установите [Postman](https://www.postman.com/)
2. Создайте новый запрос
3. Выберите метод (GET, POST, PUT, DELETE)
4. Введите URL: `http://localhost:3000/api/products`
5. Для POST/PUT добавьте JSON в Body → raw → JSON

### Вариант 3: Браузер
Для GET-запросов можно просто открыть в браузере:
- http://localhost:3000/api/products - все товары
- http://localhost:3000/api/products/1 - товар с id=1

### Вариант 4: VS Code REST Client
1. Установите расширение "REST Client"
2. Создайте файл `test.http`
3. Добавьте запросы:

```http
### Получить все товары
GET http://localhost:3000/api/products

### Получить товар по id
GET http://localhost:3000/api/products/1

### Добавить товар
POST http://localhost:3000/api/products
Content-Type: application/json

{
  "название": "Планшет iPad Pro",
  "стоимость": 89990
}

### Обновить товар
PUT http://localhost:3000/api/products/1
Content-Type: application/json

{
  "название": "Новое название",
  "стоимость": 15000
}

### Удалить товар
DELETE http://localhost:3000/api/products/2
```

---

## ✅ Выполненные требования

### Карточка товара (SASS)
- ✅ Реализована карточка с названием, описанием и фото
- ✅ Использовано 5 переменных (требовалось минимум 2)
- ✅ Использовано 2 миксина (требовалось минимум 1)
- ✅ Использована вложенная структура селекторов (BEM)
- ✅ Препроцессор: SASS

### REST API
- ✅ Просмотр всех товаров (GET /api/products)
- ✅ Просмотр товара по id (GET /api/products/:id)
- ✅ Добавление товара (POST /api/products)
- ✅ Редактирование товара (PUT /api/products/:id)
- ✅ Удаление товара (DELETE /api/products/:id)
- ✅ Объект товара содержит: id, название, стоимость
- ✅ Валидация входных данных
- ✅ Правильные HTTP статус-коды

---

## 🎯 Примеры использования

### Сценарий 1: Добавить товар
```bash
# 1. Запустите сервер
node app.js

# 2. Добавьте товар
curl -X POST http://localhost:3000/api/products \
  -H "Content-Type: application/json" \
  -d '{"название":"Клавиатура","стоимость":3500}'

# 3. Проверьте, что товар добавлен
curl http://localhost:3000/api/products
```

### Сценарий 2: Обновить товар
```bash
# 1. Обновите товар с id=1
curl -X PUT http://localhost:3000/api/products/1 \
  -H "Content-Type: application/json" \
  -d '{"стоимость":500}'

# 2. Проверьте изменения
curl http://localhost:3000/api/products/1
```

### Сценарий 3: Удалить товар
```bash
# 1. Удалите товар с id=2
curl -X DELETE http://localhost:3000/api/products/2

# 2. Проверьте, что товар удалён
curl http://localhost:3000/api/products
```

---

## 📌 Важные замечания

1. **API хранит данные в памяти** - при перезапуске сервера данные сбросятся
2. **SASS нужно компилировать** - после изменения `.scss` файла запустите `sass style.scss style.css`
3. **CORS не настроен** - если будете делать запросы с другого домена, нужно добавить CORS middleware
4. **Порт 3000** - убедитесь, что порт свободен, иначе измените в `app.js`

---

## 📧 Автор
Проект создан для демонстрации работы с SASS препроцессорами и REST API.
